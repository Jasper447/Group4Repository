CREATE UNIQUE INDEX idx_invoice_id
ON INVOICE(InvoiceId);

CREATE UNIQUE INDEX idx_customer_id
ON CUSTOMER(CustomerId);

CREATE NONCLUSTERED INDEX idx_lastname_firstname
ON CUSTOMER(LastName, FirstName);

CREATE UNIQUE INDEX idx_Mediatype
ON Mediatype(MediaTypeId);

CREATE UNIQUE INDEX idx_Artist
ON Artist(ArtistId);

CREATE UNIQUE INDEX idx_ALBUM
ON ALBUM(AlbumId, ARTIST_ArtistId);

CREATE UNIQUE INDEX idx_PLAYLIST
ON PLAYLIST(PlayListId);

CREATE UNIQUE INDEX idx_PLAYLIST_TRACK
ON PLAYLIST_TRACK(PLAYLIST_playlistID,TRACK_TrackID);